# 项目注意事项

## 项目定位
- 本仓库为 GDGN 从零重构的实施仓库（代号 `rebuild-2026`），主任务 LCO（已知药物、未见细胞系）。
- 完整计划见 `guidance/重构计划/GDGN重构计划_rebuild-2026.md`；实现与计划冲突时以计划为准，修改计划或预注册值需在 `guidance/DECISION_LOG.md` 登记。
- 旧仓库（本机 `../模型改进/GDGN` 相对路径，只读保留）：历史证据、原始数据来源与 Ver1/Ver2 归档，不要在旧仓库中开发或修改；完整本地路径不入版本控制。

## 关于 Python 使用事项
项目使用 `uv` 管理 Python 虚拟环境和依赖项，请不要直接调用 `py` 或 `python3` 命令，也不要直接调用 `pip` 来安装第三方包，`uv` 的使用方法请查阅相关 skill。

## 配置与数据纪律
- 配置文件为 `configs/*.json`，读取用标准库 `json`；预注册值（种子、门槛、预算、比例）修改前先查计划第 14/15 节并登记 DECISION_LOG。
- `data/` 下仅 `manifests/` 与 `splits/` 入 git；raw/canonical/archive/features/runs/holdout 用清单＋哈希管理，不入版本控制。
- 封存确认区（`data/holdout/`）的响应标签不得进入开发训练、调参或早停；泄漏检查是所有 split 相关代码的必测项（计划 16.5）。

## 证据等级
- 区分「本地可核对 / 文档报告（未本地复核）/ 待验证假设」三级证据；报告不得混写"规划完成"与"科学结论成立"。
- smoke 指标不进入科学结果；失败、中断与负结果一律记录，不从汇总中静默删除。
