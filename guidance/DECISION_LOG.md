# GDGN-RE 重构决策日志

## 2026-09-25 · M4/A2 首轮代码配置

**依据**：`guidance/重构计划/GDGN重构计划_rebuild-2026.md` §12、§14、§15 与 `guidance/重构计划/M4实施与验收计划_rebuild-2026.md`。本条仅登记实施选择，不改变队列、split、种子、主指标、筛选门槛或 A2 总预算。

- A1 已保存的三种子 MLP 经输入、样本、标签和指标复核后用作 R0；不重新计入 A2 新配置。若异机复核不通过，停止正式 A2 并为 R0 重训重新分配 4 配置预算。
- 首轮 `a2_execution.selected_models` 固定为 `additive` 与 `r1_k16`，双协议各三种子，共 12 次计划拟合。剩余 2 配置名额不预先消耗；启用 R1-K32、R2 或 P 前另记分支依据和取代项。
- R2 排序目标每批最多抽 64 个同药物训练对，仅为控制二次复杂度的实现上限；平局及低于噪声阈值的差异排除。噪声阈值与温度保留空值，不能在正式训练前从 outer test 决定。
- P 的同通路线性、匹配随机成员、同容量无先验对照均作为可选配置实现。正式拟合均计入 4 配置/24 次预算；对照不齐时只称数值候选，不作通路先验主张。
- 本机 `A2_SMOKE` 仅验证读取、训练、预测、保存与指标重算。其性能数值不得进入科学报告或配置选择。

## 2026-09-25 · A2 正式矩阵执行与判定

**依据**：`guidance/重构计划/GDGN重构计划_rebuild-2026.md` §14、§15 与 `guidance/重构计划/M4实施与验收计划_rebuild-2026.md` §3–4。本条仅登记已执行的正式运行、判定结果与后续分支取舍，不改变队列、split、种子、主指标、筛选门槛或 A2 总预算。

- 在冻结矩阵 `selected_models = ["additive", "r1_k16"]` 下执行 12/12 次已选配置拟合（LCO/LPO × 三种子），全部 `succeeded`；manifest hash `6029dcec2945c961d6196c325cc51c5bfb33dc440c1f0cb745acfeece74b0ff5`，git commit `03d3ae7`，12/12 `evaluate --run-dir` 重算相等。A1 八条对照预检通过，R0 复用 A1 MLP，未消耗 A2 配置名额。
- §15.1 开发筛选判定：`additive` 未通过（平均 Δmacro −0.0578，0/3 同向）；`r1_k16` 未通过（平均 Δmacro +0.0133 < +0.02，3/3 同向，平均 RMSE 比 1.0207 > 1.02）。种子平均配对差值 +0.0232，cluster bootstrap 95% CI [−0.0020, +0.0498] 跨 0；按预注册规则不得写作通过，只能记为"证据不足"。
- 机制对照：`r1_k16` 相对 `additive` 种子平均差值 +0.0776，95% CI [+0.0533, +0.1057] 不含 0，交互项有可分离贡献；相对 A1 R0 三种子均为小幅正向（+0.0066/+0.0096/+0.0108）但 CI 均跨 0。
- 后续取舍：`additive` 方向按停止规则终止；`r1_k16` 保留为数值候选并明确"筛选未通过"。剩余 2 个配置名额与 12 次拟合预算暂不消耗；若启用 `r1_k32`、R2 或 P，须先在本日志登记分支依据与取代项，并对 R2 仅用训练数据冻结噪声阈值与温度。是否以未通过筛选的探索性候选进入 B 轮，须另行登记并报告多折结果。
- 结果报告：`guidance/A2/A2_RESULT_REPORT.md`；自动候选报告：`guidance/M4/A2_CANDIDATE_REPORT.md`；台账：`guidance/EXPERIMENT_REGISTRY.md` A2 节。smoke 与单元测试不进入科学结论。

## 2026-09-25 · A2 续测分支：启用 R1-K32

**依据**：`guidance/重构计划/GDGN重构计划_rebuild-2026.md` §12、§14、§15 与 `guidance/重构计划/M4实施与验收计划_rebuild-2026.md` §3。本条登记分支取舍与配置变更，不改变队列、split、种子、主指标、筛选门槛或 A2 总预算。

- 判定依据（首轮 A2）：`additive` 已按停止规则终止；`r1_k16` 虽未通过 §15.1 筛选（平均 Δmacro +0.0133、RMSE 比 1.0207、种子聚合 CI 跨 0），但相对特征加性的差值 CI [+0.0533, +0.1057] 不含 0，是唯一有可分离机制证据的方向。选择 K=32 作为 R1 唯一未探索的单变量轴继续一次；不启用 R2（噪声阈值/温度未冻结）或 P（本体＋对照所需名额不足）。
- 配置变更：`configs/models.json` 的 `a2_execution.selected_models` 由 `["additive", "r1_k16"]` 更新为 `["additive", "r1_k16", "r1_k32"]`，`r1_k32` 移出 `optional_models`。不取代任何已运行配置；`additive`/`r1_k16` 的 12 条已保存运行保持不变、不重跑。
- 预算：新增 1 配置 × LCO/LPO × 3 种子 = 6 次拟合；执行后累计 18/24 次，剩余 6 次与 1 个配置名额；每家族 trial 上限不变（8）。
- 判定规则不变：仍按 §15.1（LCO 相对每药物 ElasticNet 平均 Δmacro ≥ +0.02、3 种子至少 2 个同向、平均 RMSE 比 ≤ 1.02），统计确认另以种子聚合 cluster bootstrap 说明。若仍未通过，按停止规则终止交互方向，以 A1 每药物 ElasticNet 为最强适用简单基线进入 B 轮确认。

## 2026-09-25 · A2 续测判定与重复执行归档

**依据**：同前条；本条登记 `r1_k32` 续测结果、重复执行的处理与后续分支状态。

- 执行：`r1_k32` 6/6 成功（LCO/LPO × 三种子；manifest `17739c76beae16a5a2fdbef9b7c7006da45e04bd5cd7be100d9afc515d1677d2`），8/8 trials 成功，6/6 `evaluate` 重算相等，运行约 1.69 GPU 小时；累计 18/24 次，余额 6 次与 1 个配置名额。
- 判定：§15.1 继续门槛**未通过**。种子平均 Δmacro +0.018531（< +0.02，差 0.0015），正向种子 3/3，平均 RMSE 比 1.014579（≤ 1.02）。逐种子增益 +0.0248/+0.0177/+0.0131，不得只取达标种子。
- 统计（种子先聚合、LCO 按细胞相关组重采样 200 次）：`r1_k32` vs 简单对照 +0.032162，95% CI [+0.004505, +0.064049] **不含 0**；vs `additive` +0.086584 CI [+0.062402, +0.116248]；vs `r1_k16` +0.008972 CI [−0.001126, +0.020489] 跨 0（K=16 与 K=32 无显著差异）。逐药物 vs 简单对照 142/205 为正；种子平均 macro：简单对照 0.5031、`r1_k32` 0.5360、`r1_k16` 0.5271、`additive` 0.4495。LPO 桥接：`r1_k32` 0.7644 > `r1_k16` 0.7485 > A1 R0 ≈ 0.7417 > Ridge 0.6194。
- 分支状态：K 轴已穷尽（注册候选 K∈{16, 32}），不再新增 R1 配置；`r1_k32` 仅可称为"未通过筛选的探索性候选"。是否登记例外并以 `r1_k32` 进入 B 轮多折确认，或按停止规则终止交互方向并交付简单基线，**待研究者登记决定**；在登记前不启动 B 轮。
- 重复执行归档：修改 `models.json` 使 run_key 变化，`--all --resume` 重跑 `additive`/`r1_k16` 共 12 条；核实其 macro/RMSE/epoch/参数量与首轮逐位一致，保留首轮为正式记录，12 条重复执行整体归档至 `data/runs/A2_duplicate_rerun_20260925/`（含 `archive_manifest.json`，未删除）。额外算力 16,746 s ≈ 4.65 GPU 小时已计入资源记录。后续追加配置一律逐条 `--model` 运行，避免 `--all` 重跑。
- 报告：`guidance/A2/A2_RESULT_REPORT.md` §12–14（含续测台账、判定、统计、归档与建议）；`guidance/M4/A2_CANDIDATE_REPORT.md` 已刷新为 18 条；`guidance/EXPERIMENT_REGISTRY.md` A2 节已更新。
